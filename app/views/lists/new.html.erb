<div class="container">
  <h1>Create a new movie list</h1>
  <div class="form">
    <%= simple_form_for [ @list] do |f| %>
      <%= f.input :name %>
      <%= f.association :movies, as: :radio_buttons %>
      <%= f.submit "Submit", class: "btn btn-primary" %>
    <% end %>
  </div>
</div>
